# Dependencies are defined in pyproject.toml.
#
# Install the package with its runtime dependencies:
#     pip install -e .
#
# Or with development tools (pytest, black, isort, flake8, mypy, bandit, safety):
#     pip install -e ".[dev]"
#
# Optional extras:
#     pip install -e ".[mesh]"   # trimesh for advanced STL ops
#     pip install -e ".[docs]"   # sphinx for building the docs
#
# This file is retained as a shim for tools and users expecting
# requirements.txt; the single source of truth is pyproject.toml.
-e .
