# ===========================================================================
# HPC Configuration — edit these variables for your cluster
# ===========================================================================

# --- Connection ---
HPC_HOST="csf3"                          # SSH alias or full hostname
HPC_USER="mchi4jw4"                      # Username on the cluster

# --- Paths ---
HPC_SCRATCH="~/scratch"                  # Scratch/work directory on cluster
# Remote case will be at: $HPC_SCRATCH/$PATIENT_ID/$RUN_NAME/

# --- Scheduler (SLURM) ---
HPC_PARTITION="multicore_small"           # SLURM partition (e.g., multicore, batch, compute)
HPC_NCORES=32                            # Number of MPI cores
HPC_WALLTIME="7-0"                       # Walltime (SLURM format: D-HH:MM:SS)

# --- OpenFOAM ---
HPC_OF_MODULE="apps/gcc/openfoam/12"     # Module load command (cluster-specific)
HPC_SOLVER="foamRun"                     # OpenFOAM solver command (foamRun for OF12, pimpleFoam for older)
HPC_SOLVER_ARGS="-solver incompressibleFluid"  # Extra solver arguments (empty for older OF)

# --- Local case ---
PATIENT_ID="VOL04"
RUN_NAME="laminar_2M_v2"
LOCAL_CASE_DIR="output/${PATIENT_ID}/${RUN_NAME}/openfoam"
